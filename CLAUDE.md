# Claude Code / Codex Agent Instructions for ny-remote-hub

Refer to [`AGENTS.md`](./AGENTS.md) for full project architecture, recent commits, and dev workflow.

## Quick Reference Commands
```bash
# Setup
python -m venv .venv; .venv\Scripts\activate (Windows) or source .venv/bin/activate (Linux/Mac)
pip install -r requirements.txt

# Run
# Check main entry point scripts or config files.

# Test
# Tests
```
